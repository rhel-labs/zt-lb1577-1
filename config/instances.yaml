---
virtualmachines:
  - name: "rhel"
    image: "rhel-10-0-07-09-25-3"
    bootloader: efi
    memory: "4G"
    cores: 1
    image_size: "40G"
    tags:
      - key: "AnsibleGroup"
        value: "bastions"
    networks:
      - default
    packages:
      - httpd
    services:
      - name: web
        ports:
          - port: 80
            protocol: TCP
            targetPort: 80
            name: web
    routes:
      - name: web
        host: rhel
        service: http
        targetPort: 80
        tls: false